════════════════════════════════════════════════════════════════════════════
                    🔧 GOOGLE OAUTH SETUP GUIDE 🔧
════════════════════════════════════════════════════════════════════════════

Based on your screenshot, you need to add ONE MORE SETTING in Google Console:

┌──────────────────────────────────────────────────────────────────────────┐
│                                                                          │
│  YOUR GOOGLE CONSOLE SCREEN (What you showed):                          │
│  ┌────────────────────────────────────────────────────────────────┐    │
│  │ Authorized JavaScript origins                                  │    │
│  │ For use with requests from a browser                          │    │
│  │                                                                │    │
│  │ URIs 1: http://localhost:8080                          ✅     │    │
│  │                                                                │    │
│  └────────────────────────────────────────────────────────────────┘    │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────┐    │
│  │ Authorized redirect URIs                                      │    │
│  │ For use with requests from a web server                       │    │
│  │                                                                │    │
│  │ URIs 1: http://localhost:8080                          ❌     │    │
│  │         ↑ THIS IS WRONG!                                      │    │
│  │                                                                │    │
│  └────────────────────────────────────────────────────────────────┘    │
│                                                                          │
└──────────────────────────────────────────────────────────────────────────┘

════════════════════════════════════════════════════════════════════════════

🎯 WHAT YOU NEED TO DO:

Step 1: Clear the wrong redirect URI
┌────────────────────────────────────────────────────────────────────────┐
│ In "Authorized redirect URIs" section:                                │
│ 1. Delete "http://localhost:8080"                                     │
│ 2. Click the trash icon next to it                                    │
└────────────────────────────────────────────────────────────────────────┘

Step 2: Add the CORRECT redirect URI
┌────────────────────────────────────────────────────────────────────────┐
│ In the empty "URIs 1" field, type:                                    │
│                                                                        │
│   http://localhost:5000/api/user/auth/google/callback                 │
│                                                                        │
│ ⚠️  IMPORTANT: Copy this EXACTLY - port 5000, not 8080!              │
└────────────────────────────────────────────────────────────────────────┘

Step 3: Save
┌────────────────────────────────────────────────────────────────────────┐
│ 1. Scroll to bottom of the page                                       │
│ 2. Click the blue "Save" button                                       │
│ 3. Wait for "OAuth client saved" message                              │
└────────────────────────────────────────────────────────────────────────┘

════════════════════════════════════════════════════════════════════════════

✅ CORRECT CONFIGURATION SHOULD LOOK LIKE THIS:

┌──────────────────────────────────────────────────────────────────────────┐
│ Authorized JavaScript origins                                            │
│ For use with requests from a browser                                     │
│                                                                          │
│ URIs 1: http://localhost:8080                                    ✅      │
│         └─ This is your FRONTEND (React app)                            │
│                                                                          │
├──────────────────────────────────────────────────────────────────────────┤
│ Authorized redirect URIs                                                │
│ For use with requests from a web server                                 │
│                                                                          │
│ URIs 1: http://localhost:5000/api/user/auth/google/callback     ✅      │
│         └─ This is your BACKEND (Node.js API)                           │
│                                                                          │
└──────────────────────────────────────────────────────────────────────────┘

════════════════════════════════════════════════════════════════════════════

🔍 WHY THIS MATTERS:

Frontend (Port 8080):
  • User clicks "Sign in with Google" button
  • Browser redirects to Google OAuth page
  • ✅ JavaScript origins allows this redirect

Backend (Port 5000):
  • Google redirects back after user approves
  • Goes to: http://localhost:5000/api/user/auth/google/callback
  • Backend creates user/token, redirects to frontend
  • ✅ Redirect URIs allows this callback

════════════════════════════════════════════════════════════════════════════

⏱️  AFTER SAVING:

Wait 5-10 minutes for Google to propagate changes, then test:

1. Go to http://localhost:8080/signup
2. Click "Sign up with Google"
3. Should redirect to Google → Select account → Back to your app (logged in)

════════════════════════════════════════════════════════════════════════════

🐛 TROUBLESHOOTING:

If you see "redirect_uri_mismatch" error:
  → Check that you entered http://localhost:5000/api/user/auth/google/callback EXACTLY
  → Wait 10 minutes and try again
  → Clear browser cache

If button does nothing:
  → Open browser console (F12) and check for errors
  → Verify backend is running on port 5000

If you get to Google but error coming back:
  → Check backend terminal for error logs
  → Verify GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET in backend/.env

════════════════════════════════════════════════════════════════════════════
